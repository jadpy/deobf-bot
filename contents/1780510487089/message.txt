local tbl = {}
for index = 1, 256 do
    tbl[index] = index 
end
table.remove(tbl, math.random(1, #tbl))
if #tbl == 0 then
    setmetatable({}, { ["__index"] = v1, ["__metatable"] = nil })
    if getfenv()[_] then return print("hi") end
    print("__")
end