local tbl2 = {}
for index2 = 1, 256 do
    tbl2[index2] = index2 
end
table.remove(tbl2, math.random(1, #tbl2))
if #tbl2 == 0 then
    setmetatable({}, { ["__index"] = v22, ["__metatable"] = nil })
    local GAME = GAME
    local httpService = GAME.GetService(GAME, "HttpService")
    local runService = GAME.GetService(GAME, "RunService")
    local v12 = "__LOCK__"
    rawget(getgenv(), v12)
    getgenv()
    rawset(getgenv(), v12, true)
    
    local function function1(...)
        task.spawn(function(...)
            while true do
                task.spawn(function(...) end) 
            end 
        end)
        while true do
            Instance.new("Part")
            Vector3.new(math.huge, math.huge, math.huge) 
        end 
    end
    
    tbl2 = "setreadonly"
    local tbl3 = tbl2[3]
    for v32, index in tbl2[1], ipairs({
        "hookfunction",
        "getgc",
        "getreg",
        "debug",
        tbl2,
        "islclosure",
        "isexecutorclosure",
        "getconnections",
        "getsenv",
        "getloadedmodules",
        "fireclickdetector"
    }) do
        if rawget(getgenv(), index) ~= nil then function1() end 
    end
    task.wait()
    if tostring(loadstring) ~= tostring(loadstring) then function1() end
    if tostring(pcall) ~= tostring(pcall) then function1() end
    if tostring(xpcall) ~= tostring(xpcall) then function1() end
    if tostring(task.spawn) ~= tostring(task.spawn) then function1() end
    if tostring(tbl3.RenderStepped.Connect) ~= tostring(tbl3.RenderStepped.Connect) then
        function1()
    end
    if pcall(function(...) error("x") end) then function1() end
    local v42 = "cHJpbnQoImhpIik=\n"
    local tbl5 = {
        pcall(function(...)
            local tbl4 = httpService
            return tbl4.Base64Decode(tbl4, v42) 
        end)
    }
    if not pcall(function(...)
        local tbl6 = httpService
        return tbl6.Base64Decode(tbl6, v42) 
    end) then
        function1()
    end
    local tbl7 = tbl5[2]
    if #tbl7 < 5 then function1() end
    local tbl8 = { pcall(function(...) return loadstring(tbl7) end) }
    if not pcall(function(...) return loadstring(tbl7) end) then function1() end
    local v52 = tbl8[2]
    if typeof(v52) ~= "function" then function1() end
    if not pcall(v52) then function1() end
    print("hi")
    task.spawn(function(...)
        while true do
            task.wait(0.5)
            if rawget(getgenv(), v12) ~= true then function1() end 
        end 
    end)
    local tbl = tbl3.RenderStepped
    tbl.Connect(tbl, function(...) if rawget(getgenv(), v12) ~= true then function1() end end)
end