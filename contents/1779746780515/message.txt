local tbl2 = {}
for index = 1, 256 do
    tbl2[index] = index 
end
table.remove(tbl2, math.random(1, #tbl2))
if #tbl2 == 0 then
    local v2 = setmetatable({}, { ["__index"] = v12, ["__metatable"] = nil })
    local tbl = GAME.Players.LocalPlayer.Character.Humanoid
    tbl.ChangeState(tbl, Enum.HumanoidStateType.Climbing)
    task.wait(1)
    tbl.GetState(tbl)
    print("hello world!")
end